// eslint-disable-next-line import/prefer-default-export
export const getLabel = (state => state.label);

export const getCurrentLabel = (state => state.currentLabel);

export const canContribute = (state => state.canContribute);
